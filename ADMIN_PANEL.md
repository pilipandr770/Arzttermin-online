# Админ-панель TerminFinder

Полнофункциональная панель управления платформой TerminFinder.

## 🎯 Возможности

### Управление пользователями
- ✅ Просмотр всех пациентов и врачей
- ✅ Поиск по имени, email, телефону
- ✅ Детальная информация о каждом пользователе
- ✅ Удаление пользователей
- ✅ Фильтрация и пагинация

### Верификация врачей
- ✅ Верификация/отмена верификации врачей
- ✅ Просмотр документов и данных врача
- ✅ Фильтр по статусу верификации

### Управление бронированиями
- ✅ Просмотр всех бронирований
- ✅ Отмена бронирований
- ✅ Статистика по бронированиям

### Аналитика и статистика
- ✅ Дашборд с ключевыми метриками
- ✅ Динамика регистраций
- ✅ Статистика бронирований
- ✅ Показатели верификации

## 🔐 Система безопасности

### Роли администраторов
1. **Super Admin** - полный доступ ко всем функциям
2. **Admin** - управление пользователями и верификацией
3. **Moderator** - только просмотр данных

### Аутентификация
- JWT токены для API запросов
- Токены хранятся в localStorage
- Автоматическая проверка прав доступа
- Защита от брутфорса (блокировка после 5 неудачных попыток)

### Права доступа (Permissions)
- `manage_users` - управление пользователями
- `manage_payments` - управление платежами
- `manage_verifications` - верификация врачей
- `view_analytics` - просмотр аналитики
- `manage_settings` - настройки системы

## 📦 Установка и настройка

### 1. Применить миграции

```bash
# Создать таблицу admins
python -m flask db upgrade
```

Если миграция не работает, можно создать таблицу вручную:

```python
python -c "from app import create_app, db; app = create_app(); ctx = app.app_context(); ctx.push(); db.session.execute(db.text('CREATE TABLE terminfinder.admins (id UUID PRIMARY KEY, username VARCHAR(80) UNIQUE NOT NULL, email VARCHAR(120) UNIQUE NOT NULL, password_hash VARCHAR(255) NOT NULL, role VARCHAR(50) NOT NULL, permissions TEXT[], is_active BOOLEAN DEFAULT true NOT NULL, two_factor_enabled BOOLEAN DEFAULT false NOT NULL, two_factor_secret VARCHAR(32), last_login TIMESTAMP, failed_login_attempts INTEGER DEFAULT 0 NOT NULL, account_locked_until TIMESTAMP, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL)')); db.session.commit(); print('Таблица admins создана!')"
```

### 2. Создать первого администратора

```bash
python create_simple_admin.py
```

Введите данные:
- **Username**: ваш логин
- **Email**: ваш email
- **Password**: пароль (минимум 8 символов)
- **Role**: выберите роль (по умолчанию super_admin)

### 3. Запустить сервер

```bash
python run.py
```

### 4. Войти в админ-панель

Откройте в браузере: `http://localhost:5000/admin/login`

Используйте созданные credentials для входа.

## 🖥️ Структура админ-панели

### Главная страница (Dashboard)
- Статистика пользователей (пациенты, врачи)
- Статистика бронирований
- Статистика верификации
- Доступные слоты
- Быстрые действия

### Пациенты (`/admin/users/patients`)
- Список всех пациентов
- Поиск и фильтрация
- Детальная информация
- Удаление пациента

### Врачи (`/admin/users/doctors`)
- Список всех врачей
- Фильтр по статусу верификации
- Верификация/отмена верификации
- Детальная информация
- Удаление врача

### Бронирования (`/admin/bookings`)
- Список всех бронирований
- Фильтр по статусу
- Отмена бронирований
- Детальная информация

### Верификация (`/admin/verifications`)
- Очередь верификации врачей
- Просмотр документов
- Одобрение/отклонение

### Аналитика (`/admin/analytics`)
- Графики и диаграммы
- Динамика роста
- Отчеты

## 🛣️ API Endpoints

### Аутентификация
```
POST /api/admin/login
Body: { "username": "admin", "password": "password" }
Response: { "token": "JWT_TOKEN", "admin": {...} }
```

### Статистика
```
GET /api/admin/dashboard/stats
Headers: Authorization: Bearer JWT_TOKEN
Response: { "users": {...}, "bookings": {...}, "verification": {...}, "slots": {...} }
```

### Пациенты
```
GET /api/admin/patients?page=1&per_page=20&search=query
GET /api/admin/patients/<id>
DELETE /api/admin/patients/<id>
```

### Врачи
```
GET /api/admin/doctors?page=1&per_page=20&search=query&verified=true
GET /api/admin/doctors/<id>
POST /api/admin/doctors/<id>/verify
Body: { "verified": true }
DELETE /api/admin/doctors/<id>
```

### Бронирования
```
GET /api/admin/bookings?page=1&per_page=20&status=confirmed
GET /api/admin/bookings/<id>
DELETE /api/admin/bookings/<id>
```

## 🎨 Дизайн

- **Framework**: Bootstrap 5
- **Icons**: Bootstrap Icons
- **Colors**: Профессиональная синяя цветовая схема
- **Responsive**: Адаптивный дизайн для всех устройств
- **Dark Sidebar**: Темная боковая панель с контрастными элементами

## 📁 Структура файлов

```
app/
├── models/
│   └── admin.py                    # Модель администратора
├── routes/
│   └── admin.py                    # Роуты админ-панели
└── templates/
    └── admin/
        ├── base.html               # Базовый шаблон с сайдбаром
        ├── login.html              # Страница входа
        ├── dashboard.html          # Главная страница
        ├── patients.html           # Список пациентов
        ├── doctors.html            # Список врачей
        ├── patient_detail.html     # Детали пациента (TODO)
        ├── doctor_detail.html      # Детали врача (TODO)
        ├── bookings.html           # Бронирования (TODO)
        ├── verifications.html      # Верификация (TODO)
        └── analytics.html          # Аналитика (TODO)
```

## 🔄 Следующие шаги

### В разработке
- [ ] Страница детальной информации о пациенте
- [ ] Страница детальной информации о враче
- [ ] Страница управления бронированиями
- [ ] Страница верификации с загрузкой документов
- [ ] Страница аналитики с графиками
- [ ] Страница настроек системы
- [ ] Двухфакторная аутентификация (2FA)
- [ ] История действий администратора (audit log)
- [ ] Email уведомления администраторам
- [ ] Экспорт данных (CSV, Excel)

### Планируемые функции
- [ ] Управление платежами и подписками
- [ ] Система штрафов за пропуски
- [ ] Управление депозитами
- [ ] Массовые операции (bulk actions)
- [ ] Роль Support для службы поддержки
- [ ] API для мобильной админки

## 🚀 Production Checklist

Перед развертыванием в production:

1. ✅ Создать super_admin аккаунт
2. ✅ Настроить JWT_SECRET_KEY в переменных окружения
3. ⚠️ Включить HTTPS для безопасной передачи токенов
4. ⚠️ Настроить rate limiting для API
5. ⚠️ Добавить CORS политику
6. ⚠️ Настроить логирование действий администратора
7. ⚠️ Включить двухфакторную аутентификацию
8. ⚠️ Регулярно обновлять пароли админов
9. ⚠️ Настроить бэкапы базы данных
10. ⚠️ Мониторинг подозрительной активности

## 📝 Примечания

- Пароли хешируются с помощью bcrypt
- JWT токены действительны 24 часа
- После 5 неудачных попыток входа аккаунт блокируется на 30 минут
- Все действия должны логироваться (планируется)
- Рекомендуется использовать 2FA для super_admin (планируется)

## 🐛 Известные проблемы

- Модель Admin имеет больше полей, чем создано в базе (будет исправлено в миграции)
- Детальные страницы пользователей еще не реализованы
- Страница аналитики требует интеграции с графиками

## 💡 Советы

1. **Безопасность**: Всегда используйте сильные пароли для админов
2. **Резервное копирование**: Регулярно делайте бэкапы таблицы admins
3. **Мониторинг**: Следите за логами входов в админку
4. **Права доступа**: Давайте минимальные необходимые права
5. **Обновления**: Регулярно обновляйте зависимости

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте, что таблица `terminfinder.admins` создана
2. Проверьте, что администратор создан в базе
3. Проверьте JWT_SECRET_KEY в переменных окружения
4. Проверьте логи Flask приложения
5. Откройте браузерную консоль для ошибок JavaScript

---

**Версия**: 1.0.0  
**Дата**: 04.02.2026  
**Автор**: TerminFinder Team
